<section class="vilkaarvi" id="vilka-ar-vi">
    
    <div id="studentGrid" class="content grid grid-flow-row grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-s gap-y-s justify-items-center">
        <h2>Vilka är vi?</h2>
        {% import 'macros/cards.njk' as cards %}

        {% for student in studenter %}
            
        {{cards.person(student)}}

        {% endfor %}

    </div>
</section>